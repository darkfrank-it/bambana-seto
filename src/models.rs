
#[derive(Debug)]
pub struct Session {

    pub window_pid: u32,
    pub window_title: String,
    pub process_name: String,
    pub start_time: String,
    pub end_time: String,
    pub alias: String,
}
